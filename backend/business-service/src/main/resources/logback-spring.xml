<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- logfmt 便于本机阅读和采集端解析；MDC 中只放内部 ID，不放令牌、问题正文或联系方式。 -->
    <property name="CONSOLE_PATTERN"
              value="ts=%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} level=%-5level service=${spring.application.name:-business-service} requestId=%X{requestId:-} userId=%X{userId:-} conversationId=%X{conversationId:-} eventId=%X{eventId:-} logger=%logger{36} msg=&quot;%replace(%msg){'[\r\n]+',' '}&quot;%n"/>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder><pattern>${CONSOLE_PATTERN}</pattern><charset>UTF-8</charset></encoder>
    </appender>
    <root level="INFO"><appender-ref ref="CONSOLE"/></root>
</configuration>
